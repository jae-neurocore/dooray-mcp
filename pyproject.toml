[project]
name = "dooray-mcp"
version = "0.1.0"
description = "Dooray MCP server for project management"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
dooray-mcp = "dooray_mcp.server:main"
